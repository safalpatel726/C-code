#include <stdio.h>

int main() {
    int min = 1, max = 100;
    for (int i = 1; i <= 100; i++) {
        if (i > max) max = i;
        if (i < min) min = i;
    }
    printf("Largest = %d\nSmallest = %d\n", max, min);
    return 0;
}
