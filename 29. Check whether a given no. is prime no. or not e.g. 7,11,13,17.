#include <stdio.h>

int main() {
    int num = 7, count = 0;
    for (int i = 1; i <= num; i++) {
        if (num % i == 0) count++;
    }
    if (count == 2) printf("%d is Prime\n", num);
    else printf("%d is Not Prime\n", num);
    return 0;
}
