#include <stdio.h>

int main() {
    int a = 12, b = 45, c = 23, max, min;
    
    max = a;
    if (b > max) max = b;
    if (c > max) max = c;

    min = a;
    if (b < min) min = b;
    if (c < min) min = c;

    printf("Largest = %d, Smallest = %d\n", max, min);
    return 0;
}
