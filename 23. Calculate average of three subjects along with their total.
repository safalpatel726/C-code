#include <stdio.h>

int main() {
    float s1 = 85, s2 = 90, s3 = 80, total, avg;
    total = s1 + s2 + s3;
    avg = total / 3.0;
    printf("Total = %.2f\nAverage = %.2f\n", total, avg);
    return 0;
}
