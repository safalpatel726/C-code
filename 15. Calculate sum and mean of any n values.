#include <stdio.h>

int main() {
    int n = 4;
    float values[4] = {10, 20, 30, 40}, sum = 0;
    for (int i = 0; i < n; i++) sum += values[i];
    printf("Sum = %.2f\nMean = %.2f\n", sum, sum / n);
    return 0;
}
