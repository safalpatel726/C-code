#include <stdio.h>

int main() {
    int num = 351;
    while (num > 0) {
        printf("%d", num % 10);
        num /= 10;
        if (num > 0) printf(", ");
    }
    printf("\n");
    return 0;
}
